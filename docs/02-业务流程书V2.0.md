# JCMonitoring 业务流程书 V2.0

> **文档版本**: V2.0  
> **设计原则**: 领域驱动设计（DDD）、事件驱动、状态机严格校验  
> **编制日期**: 2026-04-22

---

## 1. 多租户数据隔离流程

### 1.1 租户识别与切换流程

```mermaid
sequenceDiagram
    participant Client as 客户端
    participant Middleware as 多租户中间件
    participant TenantStore as ITenantStore
    participant CurrentTenant as ICurrentTenant
    participant DbContext as DbContext
    participant DB as PostgreSQL

    Client->>Middleware: HTTP Request<br/>Header: __tenant=tenant-id<br/>或 Subdomain: tenant1.jcmonitor.com
    Middleware->>TenantStore: 解析租户标识
    alt 租户存在且有效
        TenantStore-->>Middleware: TenantConfiguration
        Middleware->>CurrentTenant: Change(tenantId)
        CurrentTenant->>DbContext: 设置全局过滤器 TenantId
        DbContext->>DB: 执行查询（自动附加 WHERE TenantId = ?）
        DB-->>DbContext: 返回租户隔离数据
        DbContext-->>Client: 响应数据
    else 租户不存在或已过期
        TenantStore-->>Middleware: null / expired
        Middleware-->>Client: 400 Bad Request<br/>错误码: TENANT_NOT_FOUND
    else Host 端请求（无租户标识）
        Middleware->>CurrentTenant: Change(null)
        DbContext->>DB: 执行查询（Host 视角，可跨租户）
    end
```

### 1.2 宿主管理租户数据流程

```mermaid
flowchart TD
    A[Host 管理员登录] --> B{请求类型}
    B -->|查询租户列表| C[禁用多租户过滤器<br/>DisableMultiTenancy]
    C --> D[查询 AbpTenants 表]
    D --> E[返回所有租户数据]
    
    B -->|管理指定租户数据| F[切换至目标租户上下文<br/>ICurrentTenant.Change]
    F --> G[以该租户身份执行操作]
    G --> H[操作结果归属该租户]
    
    B -->|创建新租户| I[创建 Tenant 记录]
    I --> J[初始化数据库 Schema]
    J --> K[创建默认管理员账号]
    K --> L[分配默认角色与权限]
    L --> M[发布 TenantInitializedDomainEvent]
    M --> N[异步初始化字典/模板数据]
```

### 1.3 数据隔离层级

| 层级 | 隔离方式 | 说明 |
|------|----------|------|
| L1 - 完全隔离 | 数据库-per-租户 | 每个租户独立数据库，通过 `ConnectionString` 切换（未来扩展） |
| L2 - Schema 隔离 | Schema-per-租户 | 同一数据库，不同 Schema（未来扩展） |
| L3 - 行级隔离 | TenantId 字段过滤 | **当前实现**，所有业务表含 `TenantId`，全局查询过滤器自动附加 |

**全局查询过滤器自动附加条件**：
```sql
-- 任意业务查询自动转换为：
SELECT * FROM "JcBusi_Projects" 
WHERE "IsDeleted" = false 
  AND "TenantId" = '当前租户ID'
```

**Host 端跨租户查询**：
```csharp
using (_dataFilter.Disable<IMultiTenant>())
{
    // 查询所有租户数据
    var allProjects = await _projectRepository.GetListAsync();
}
```

---

## 2. 预警状态机与领域事件流

### 2.1 预警状态机定义

```mermaid
stateDiagram-v2
    [*] --> 未处理: 触发预警
    
    未处理 --> 处理中: 分配处理人
    未处理 --> 已关闭: 自动关闭/撤销/误报
    
    处理中 --> 已处理: 提交处理方案
    处理中 --> 已关闭: 忽略/误报确认
    
    已处理 --> 已确认: 管理员确认
    已处理 --> 处理中: 驳回处理结果
    
    已确认 --> [*]: 归档
    已关闭 --> [*]: 归档
```

### 2.2 状态转换规则表

| 当前状态 | 允许操作 | 目标状态 | 触发条件 | 领域事件 |
|----------|----------|----------|----------|----------|
| 未处理 | 分配处理人 | 处理中 | `AssignHandler()` | `WarningAssignedDomainEvent` |
| 未处理 | 关闭 | 已关闭 | `Close("误报")` | `WarningClosedDomainEvent` |
| 未处理 | 系统自动 | 已关闭 | 超过7天未处理且为提示级别 | `WarningAutoClosedDomainEvent` |
| 处理中 | 提交方案 | 已处理 | `SubmitSolution()` | `WarningHandledDomainEvent` |
| 处理中 | 关闭 | 已关闭 | `Close("忽略")` | `WarningClosedDomainEvent` |
| 已处理 | 确认 | 已确认 | `Confirm()` | `WarningConfirmedDomainEvent` |
| 已处理 | 驳回 | 处理中 | `Reject()` | `WarningRejectedDomainEvent` |

### 2.3 预警处理完整领域事件流

```mermaid
sequenceDiagram
    participant Data as 监测数据
    participant P as Point聚合根
    participant WDS as WarningDomainService
    participant WR as WarningRecord
    participant DB as 数据库
    participant DE as 领域事件总线
    participant Hub as SignalR Hub
    participant Notify as 通知服务

    Data->>P: 更新监测值
    P->>WDS: 请求阈值判定
    WDS->>DB: 查询前次监测值
    DB-->>WDS: 返回历史数据
    WDS->>WDS: 计算变化率/累计值
    WDS->>WDS: 对比阈值
    
    alt 触发阈值
        WDS->>WR: 创建 WarningRecord
        WR->>WR: 设置状态=未处理
        WR->>DE: 发布 WarningTriggeredDomainEvent
        DE->>P: 更新 Point.CurrentWarningLevel
        DE->>Hub: 推送实时预警
        DE->>Notify: 发送站内通知
        alt 三级预警
            DE->>Notify: 发送短信/邮件告警
        end
    else 未触发阈值
        WDS->>P: 更新统计值
    end
```

### 2.4 各状态下业务约束

**未处理（HandleStatus = 0）**：
- 测点 `ActiveWarningCount` +1
- 系统发送通知给项目相关人员
- 允许：分配处理人、直接关闭
- 禁止：提交处理方案（必须先分配）

**处理中（HandleStatus = 1）**：
- 记录 `HandlerId`、`HandlerName`
- 锁定该预警，防止重复分配
- 允许：提交方案、关闭
- 禁止：重新分配（需先释放）

**已处理（HandleStatus = 2）**：
- 记录 `HandleTime`、`HandleSolution`、`HandleResult`
- 允许：管理员确认、驳回
- 禁止：修改处理方案（需驳回后重新提交）

**已确认（HandleStatus = 3 / 业务上合并到已处理闭环）**：
- 记录 `ConfirmTime`、`ConfirmerId`
- 测点 `ActiveWarningCount` -1
- 触发数据归档标记

**已关闭（HandleStatus = 4）**：
- 测点 `ActiveWarningCount` -1
- 记录关闭原因
- 不可再变更状态

---

## 3. 设备校准领域流程

### 3.1 校准生命周期状态机

```mermaid
stateDiagram-v2
    [*] --> 正常: 设备入库
    正常 --> 已借出: 借出到项目
    已借出 --> 正常: 归还
    
    正常 --> 校准中: 执行校准
    校准中 --> 正常: 校准完成
    
    正常 --> 维修中: 故障送修
    维修中 --> 正常: 维修完成
    
    正常 --> 停用: 暂停使用
    停用 --> 正常: 恢复使用
    
    正常 --> 报废: 报废处理
    已借出 --> 报废: 强制报废
    维修中 --> 报废: 无法修复
    停用 --> 报废: 永久停用
    报废 --> [*]
```

### 3.2 校准提醒与执行流程

```mermaid
sequenceDiagram
    participant Admin as 设备管理员
    participant App as CompanyDeviceAppService
    participant Dev as CompanyDevice
    participant HF as Hangfire Job
    participant Notify as 通知服务
    participant DB as 数据库

    rect rgb(240,248,255)
        Note over Admin,DB: 设备入库阶段
        Admin->>App: 创建设备档案
        App->>Dev: 初始化设备
        Dev->>Dev: 设置 CalibrationDate = null
        Dev->>Dev: 设置 NextCalibrationDate = null
        Dev->>DB: 保存设备记录
    end

    rect rgb(255,250,240)
        Note over Admin,DB: 首次校准注册
        Admin->>App: 执行首次校准
        App->>Dev: Calibrate(date, nextDate, accuracy)
        Dev->>Dev: 更新 CalibrationDate
        Dev->>Dev: 计算 NextCalibrationDate
        Dev->>Dev: 状态 = 正常
        Dev->>DB: 保存
        App->>HF: 注册校准提醒任务
    end

    rect rgb(240,255,240)
        Note over HF,Notify: 定时提醒阶段
        HF->>HF: 每日扫描 NextCalibrationDate
        alt NextCalibrationDate <= Now + 7天
            HF->>Notify: 发送校准提醒
            Notify->>Admin: SignalR推送 + 站内消息
        end
    end

    rect rgb(255,240,240)
        Note over Admin,DB: 再次校准阶段
        Admin->>App: 执行校准
        App->>Dev: Calibrate(newDate, newNextDate, newAccuracy)
        Dev->>Dev: 状态校验：已借出/维修中禁止校准
        Dev->>Dev: 更新 CalibrationDate
        Dev->>Dev: 计算 NextCalibrationDate
        Dev->>DB: 保存
        App->>HF: 重置下次提醒任务
    end
```

### 3.3 校准业务规则

| 规则编号 | 规则描述 | 校验位置 |
|----------|----------|----------|
| DEV-CAL-01 | 已借出设备禁止校准 | DeviceManager 领域服务 |
| DEV-CAL-02 | 维修中设备禁止校准 | DeviceManager 领域服务 |
| DEV-CAL-03 | 报废设备禁止任何操作 | CompanyDevice 实体方法 |
| DEV-CAL-04 | `NextCalibrationDate` 必须晚于 `CalibrationDate` | CompanyDevice 实体方法 |
| DEV-CAL-05 | 校准提醒提前7天触发 | Hangfire Job 逻辑 |
| DEV-CAL-06 | 超期未校准设备状态自动标记为"需校准" | Hangfire Job 逻辑 |

---

## 4. 监测数据入库与预警判定流程

### 4.1 整体流程图

```mermaid
graph TD
    A1[监测仪器自动采集]
    A2[人工录入终端]
    A3[Excel批量导入]
    B1[数据格式校验]
    B2[数据质量检查]
    B3[保存MonitoringData]
    B4[发布MonitoringDataCollectedDomainEvent]
    C1[WarningDomainService.EvaluateAsync]
    C2[查询Point阈值配置]
    C3[查询前次监测值]
    C4[计算变化率与累计值]
    C5{是否触发预警}
    D1[创建WarningRecord]
    D2[更新Point统计值]
    D3[更新Point预警状态]
    D4[发布WarningTriggeredDomainEvent]
    E1[SignalR实时推送]
    E2[站内消息通知]
    E3[短信邮件告警]
    E4[大屏展示更新]
    F1[返回错误不入库]
    G1[流程结束]

    subgraph 数据输入层
        A1
        A2
        A3
    end

    subgraph 应用服务层
        B1
        B2
        B3
        B4
    end

    subgraph 领域服务层
        C1
        C2
        C3
        C4
        C5
    end

    subgraph 事件处理层
        D1
        D2
        D3
        D4
    end

    subgraph 推送与通知层
        E1
        E2
        E3
        E4
    end

    A1 --> B1
    A2 --> B1
    A3 --> B1
    B1 -->|格式异常| F1
    F1 --> G1
    B1 --> B2
    B2 -->|质量异常| B3
    B2 -->|正常| B3
    B3 --> B4
    B4 --> C1
    C1 --> C2
    C2 --> C3
    C3 --> C4
    C4 --> C5
    C5 -->|是| D1
    C5 -->|否| D2
    D1 --> D3
    D3 --> D4
    D4 --> E1
    D4 --> E2
    D4 -->|三级预警| E3
    E1 --> E4
    D2 --> G1
    E4 --> G1
    E2 --> G1
    E3 --> G1
```

### 4.2 数据校验规则

| 校验阶段 | 校验项 | 失败处理 |
|----------|--------|----------|
| 格式校验 | `MonitoringValue` 必须为有效数值 | 返回 400，记录校验日志 |
| 格式校验 | `MonitoringTime` 不得晚于当前时间 | 返回 400，标记为异常时间 |
| 格式校验 | `PointId` 必须存在且属于当前租户 | 返回 404 |
| 质量检查 | 值超出物理量程范围 | 标记 `DataQuality = 异常` |
| 质量检查 | 与前次值偏差 > 50% | 标记 `DataQuality = 可疑` |
| 质量检查 | 采集时间间隔 < 配置频率 | 标记 `DataQuality = 可疑` |

### 4.3 预警判定逻辑（WarningDomainService）

```csharp
// 伪代码描述判定逻辑
public async Task<WarningRecord?> EvaluateAsync(MonitoringData data)
{
    var point = await _pointRepository.GetAsync(data.PointId);
    var previousData = await _monitoringDataRepository
        .GetLastDataBeforeAsync(data.PointId, data.MonitoringTime);
    
    decimal? previousValue = previousData?.MonitoringValue;
    decimal currentValue = data.MonitoringValue;
    
    // 1. 阈值判定
    if (point.AlarmThreshold.HasValue && currentValue >= point.AlarmThreshold.Value)
        return CreateWarningRecord(data, WarningType.Threshold, WarningLevel.Level3Danger);
    
    if (point.WarningThreshold.HasValue && currentValue >= point.WarningThreshold.Value)
        return CreateWarningRecord(data, WarningType.Threshold, WarningLevel.Level2Warning);
    
    // 2. 变化率判定
    if (previousValue.HasValue && point.ChangeRateThreshold.HasValue)
    {
        var changeRate = Math.Abs((currentValue - previousValue.Value) / previousValue.Value * 100);
        if (changeRate >= point.ChangeRateThreshold.Value)
            return CreateWarningRecord(data, WarningType.ChangeRate, WarningLevel.Level1Notice);
    }
    
    // 3. 累计值判定
    if (point.CumulativeThreshold.HasValue)
    {
        var initialValue = await GetInitialValueAsync(data.PointId);
        var cumulativeChange = Math.Abs(currentValue - initialValue);
        if (cumulativeChange >= point.CumulativeThreshold.Value)
            return CreateWarningRecord(data, WarningType.Cumulative, WarningLevel.Level2Warning);
    }
    
    return null; // 未触发预警
}
```

### 4.4 领域事件发布顺序

```
1. MonitoringDataCollectedDomainEvent（本地事件）
   └→ 触发预警判定
   
2. WarningTriggeredDomainEvent（分布式事件，如触发预警）
   ├→ Handler 1: 更新 Point 当前预警级别
   ├→ Handler 2: SignalR 推送到前端
   ├→ Handler 3: 创建站内通知 Message
   └→ Handler 4: 三级预警时发送短信/邮件

3. PointStatisticsUpdatedDomainEvent（本地事件）
   └→ 更新 Point 最大/最小/平均值
```

### 4.5 事务边界

**工作单元配置**：
- 监测数据入库 + 预警记录创建必须在同一事务中
- SignalR 推送和短信发送在事务提交后通过分布式事件异步执行
- 避免在领域事件处理器中引入外部调用阻塞事务

```csharp
// Application Service 中
public async Task CreateAsync(CreateMonitoringDataDto input)
{
    var data = await _monitoringDataManager.CreateAsync(input);
    await _repository.InsertAsync(data);
    // MonitoringDataCollectedDomainEvent 自动发布
    // WarningTriggeredDomainEvent 如有预警则自动发布
    // 事务提交后，事件处理器异步执行推送
}
```

---

## 5. 登录与权限业务流程

### 5.1 登录验证流程

```mermaid
sequenceDiagram
    participant User as 用户
    participant API as AccountController
    participant App as AccountAppService
    participant TM as TenantManager
    participant UM as UserManager
    participant OM as OpenIddict
    participant Cache as Redis

    User->>API: POST /api/account/login<br/>{account, password, __tenant?}
    API->>App: LoginAsync(input)
    
    alt 请求含租户标识
        App->>TM: 解析并切换租户
        TM-->>App: TenantContext
    else 无租户标识
        App->>TM: Host 默认上下文
    end
    
    App->>UM: 验证用户名密码
    UM->>UM: MD5/AES 解密比对
    
    alt 验证成功
        UM-->>App: IdentityUser
        App->>App: 查询用户角色与权限
        App->>Cache: 缓存权限列表（5分钟）
        App->>OM: 签发 AccessToken + RefreshToken
        OM-->>App: TokenResponse
        App-->>API: LoginOutputDto
        API-->>User: {userId, loginToken, tenantId, isAdmin, permissions}
    else 验证失败
        UM-->>App: IdentityResult.Failed
        App-->>API: BusinessException("AUTH:001", "用户名或密码错误")
        API-->>User: 401 Unauthorized
    end
```

### 5.2 权限加载与校验流程

```mermaid
flowchart TD
    A[用户登录成功] --> B[查询用户角色列表]
    B --> C[查询角色-权限映射]
    C --> D[扁平化权限标识数组]
    D --> E[存入 Redis 缓存]
    E --> F[返回给前端]
    
    G[前端请求API] --> H[携带 JWT Token]
    H --> I[OpenIddict 验证 Token]
    I --> J[从 Redis 加载权限缓存]
    J --> K[ABP 权限拦截器校验]
    K -->|有权限| L[执行应用服务]
    K -->|无权限| M[返回 403 Forbidden]
```

---

## 6. 数据归档流程

### 6.1 预警记录归档

```mermaid
flowchart TD
    A[Hangfire ArchiveJob 触发] --> B[查询已确认/已关闭且超过30天的预警]
    B --> C{是否达到归档条件}
    C -->|是| D[插入到 WarningRecordArchive 表]
    D --> E[删除原 WarningRecord 记录]
    E --> F[记录归档日志]
    C -->|否| G[跳过]
```

### 6.2 监测数据归档

```mermaid
flowchart TD
    A[Hangfire DataArchiveJob 触发] --> B[查询超过1年的监测数据]
    B --> C[按年月分区迁移到历史表]
    C --> D[更新 PointStatistics 汇总表]
    D --> E[释放主表空间]
```

---

## 7. 租户初始化流程

```mermaid
sequenceDiagram
    participant Host as Host 管理员
    participant TenantApp as TenantAppService
    participant TenantMgr as TenantManager
    participant DB as PostgreSQL
    participant EventBus as 事件总线
    participant Seed as 种子数据服务

    Host->>TenantApp: CreateAsync(TenantCreateDto)
    TenantApp->>TenantMgr: CreateAsync(name)
    TenantMgr->>DB: 插入 AbpTenants 记录
    DB-->>TenantMgr: 返回 Tenant
    
    TenantApp->>DB: 创建默认管理员 IdentityUser
    TenantApp->>DB: 创建默认角色 IdentityRole
    TenantApp->>DB: 分配默认权限
    
    TenantApp->>EventBus: 发布 TenantInitializedDomainEvent
    EventBus->>Seed: 异步执行
    Seed->>DB: 初始化系统字典
    Seed->>DB: 初始化监测项目类型
    Seed->>DB: 初始化默认组织架构
    
    TenantApp-->>Host: TenantDto
```

**默认初始化内容**：
1. 系统字典（项目状态、设备类型、预警级别等）
2. 监测项目类型（位移监测、沉降监测、应力监测等）
3. 默认组织架构（根节点 = 租户名称）
4. 快捷模块配置
5. 系统设置（Logo、项目名称等）
